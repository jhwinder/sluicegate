[project]
name = "sluicegate-core"
version = "0.1.0-beta"
description = "SluiceGate Open Core Beta"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "pyyaml>=6.0.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sg_core"]
